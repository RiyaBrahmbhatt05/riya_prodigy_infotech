document.addEventListener('DOMContentLoaded', () => {
  new Typed("#typed", {
    strings: ["From Algorithms to Interfaces — I Bring Ideas to Life."],
    typeSpeed: 40,
    backSpeed: 20,
    showCursor: true,
    smartBackspace: true,
    loop: false
  });
});
